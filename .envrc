#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "ds_digital_ads")"
[ "$CONDA_DEFAULT_ENV" = "ds_digital_ads" ] || echo "Have you run \`make install\`?"
